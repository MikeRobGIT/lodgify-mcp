{
  "name": "@mikerob/lodgify-mcp",
  "version": "0.1.25",
  "description": "Model Context Protocol server for Lodgify Public API - Expose Lodgify endpoints as MCP tools for AI assistants",
  "type": "module",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "bin": {
    "lodgify-mcp": "dist/server.js"
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "scripts": {
    "dev": "bun src/server.ts",
    "build": "tsc --strict && chmod +x dist/server.js",
    "start": "node dist/server.js",
    "start:http": "bun src/server-http.ts",
    "start:http:prod": "bun dist/server-http.js",
    "check": "bun run lint --fix && bun run typecheck && bun run format && bun run build && bun run test",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "lint": "biome check .",
    "lint:fix": "biome check . --fix",
    "lint:src": "biome check src/ --fix --unsafe",
    "format": "biome format --write .",
    "format:src": "biome format --write src/",
    "typecheck": "tsc --noEmit",
    "prepare": "husky",
    "pre-commit": "(bun run lint:src || echo '⚠️  Linting warnings found but continuing...') && bun run format:src",
    "pre-push": "bun test && bun run build",
    "sync-version": "node scripts/sync-version.js",
    "docker:mcp:build": "docker build -f Dockerfile.mcp -t lodgify-mcp:mcp .",
    "docker:http:build": "docker build -f Dockerfile.http -t lodgify-mcp:http ."
  },
  "keywords": [
    "mcp",
    "modelcontextprotocol",
    "lodgify",
    "api",
    "property-management",
    "vacation-rental",
    "booking",
    "hospitality",
    "ai-tools",
    "claude"
  ],
  "author": "Your Name",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MikeRobGIT/lodgify-mcp.git"
  },
  "bugs": {
    "url": "https://github.com/MikeRobGIT/lodgify-mcp/issues"
  },
  "homepage": "https://github.com/MikeRobGIT/lodgify-mcp#readme",
  "engines": {
    "node": ">=18",
    "bun": ">=1.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "@types/cors": "^2.8.19",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^4",
    "i18next": "^23.16.8",
    "pino": "^9.9.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.3",
    "@types/bun": "^1.3.9",
    "@types/express": "^4",
    "@types/node": "^24.10.13",
    "husky": "^9.1.7",
    "pino-pretty": "^13.1.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
